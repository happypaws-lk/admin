# Base URL of the HappyPaws backend API service
NEXT_PUBLIC_API_URL=http://localhost:5000

# Cookie domain scope (leave empty for localhost, set to .happypaws.lk in production)
AUTH_COOKIE_DOMAIN=

# Enforce HTTPS-only secure cookies (set to true in production, false for local HTTP development)
AUTH_COOKIE_SECURE=false
